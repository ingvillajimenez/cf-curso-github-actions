name: Example of a workflow with a variable

on: [push]

jobs:
  test-job:
    runs-on: ubuntu-latest

    env:
      MY_VAR: "Mi variable de entorno"

    steps:
      - name: List files
        run: ls -la

      - name: Print environment variable
        run: echo "Value is $MY_VAR"
